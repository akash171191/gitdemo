pipeline            //jenkins always start with pipeline keyword
{
    agent any       //where to run the jon
    stages           // It contains all the stages
    {
        stage ('Display hello')
        {
            steps { sh 'echo hi_jenkins' }
        }
    }
}